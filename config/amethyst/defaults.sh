#!/usr/bin/env bash
# Restore Amethyst defaults if they were lost
# Your keybindings are already stored in macOS defaults — this is just a safety net
echo "Amethyst preferences managed by macOS defaults — no file config needed"
